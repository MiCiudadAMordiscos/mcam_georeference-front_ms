// el type es porque es un tipo xd
import type { UserPoint } from "@services/fetch_near_restaurants";
import { createMap, placeNearRestaurants } from "@components/map";

// crear el mapa
const map = createMap();

